Optional GLB for roofing demos

Drop a model here as `roofing-hero.glb` (or any name) and set `assets.heroGlbUrl` in that demo’s stored JSON to `/models/your-file.glb`.

If the file is missing, the hero still uses the procedural roof + shader ground + scroll camera.
